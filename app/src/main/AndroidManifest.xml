<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        INTERNET permission is ONLY required for the OPTIONAL WebView / browser
        feature that opens the real KNPortal website (see MainActivity.java and
        the "Optional KNPortal Web URL" section of the write-up).
        It is NOT required for the basic demo login, so it is commented out.
        Uncomment this line if you enable the WebView option.
    -->
    <!-- <uses-permission android:name="android.permission.INTERNET" /> -->

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_knportal_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_knportal_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.KNPortal">

        <!-- Splash/Welcome screen: this is the screen that opens first when the app launches -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login screen, opened when CONTINUE is tapped on the splash screen -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- Dashboard screen shown after a "successful" demo login -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false" />

        <!-- Courses screen, opened from the Dashboard -->
        <activity
            android:name=".CoursesActivity"
            android:exported="false" />

        <!-- Notices screen, opened from the Dashboard -->
        <activity
            android:name=".NoticesActivity"
            android:exported="false" />

        <!-- Profile screen, opened from the Dashboard -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <!--
            HomeActivity: leftover from an earlier demo build, kept as-is
            and still declared here so it continues to compile, but it is
            no longer part of the KNP Portal navigation flow above.
        -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

    </application>

</manifest>
